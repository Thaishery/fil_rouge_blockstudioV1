 {% extends 'base.html.twig' %}


     {% block metaTitle %}
         " Plateforme "
     {% endblock %}


     {% block title %}
         Plateforme
     {% endblock %}
     
     {% block body %}

         <h1> Plateforme </h1>
     
     <table class="table table-dark table-hover">

             <tbody>

                 <tr>
                     <th> Id </th>
                     <td> {{ plateforme.id }} </td>
                 </tr>

                 <tr>
                     <th> Nom </th>
                     <td>{{ plateforme.name }}</td>
                 </tr>

                 <tr>
                     <th> Lien </th>
                     <td>{{ plateforme.link }}</td>
                 </tr>

             </tbody>
             
         </table>
          
         <a class="btn btn-primary btn-lg btn-block" href="{{ path('plateforme_edit', {'id': plateforme.id, 'login': app.user.login}) }}"> Modifier </a>

         {{ include('plateforme/_delete_form.html.twig') }} 

         <a class="btn btn-primary btn-lg btn-block" href="{{ path('plateforme_index', {'login': app.user.login}) }}"> Retour à la liste </a>
  

     {% endblock %}

