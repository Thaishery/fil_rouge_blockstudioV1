{% extends 'base.html.twig' %}


     {% block metaTitle %}
         " Accueil "
     {% endblock %}


     {% block title %}
         Accueil
     {% endblock %}

     {% block body %}

         <h1> Accueil </h1>

     <table class="table table-dark table-hover">

         <tbody>

             <tr>
                 <th> Id </th>
                 <td>{{ home.id }}</td>
             </tr>

             <tr>
                 <th> Titre </th>
                 <td>{{ home.title }}</td>
             </tr>

             <tr>
                 <th> Photo </th>
                 <td>{{ home.picture }}</td>
             </tr>

             <tr>
                 <th> Courte description </th>
                 <td>{{ home.shortDesc }}</td>
             </tr>

             <tr>
                 <th> Longue description </th>
                 <td>{{ home.longDesc }}</td>
             </tr>

             <tr>
                 <th> Est actif </th>
                 <td>{{ home.isActive ? 'Oui' : 'Non' }}</td>
             </tr>

         </tbody>

     </table>


         



















         <a class="btn" href="{{ path('home_index') }}"> Revenir à la liste </a>

         <a class="btn" href="{{ path('home_edit', {'id': home.id}) }}"> Modifier </a>

         {{ include('home/_delete_form.html.twig') }}

 {% endblock %}
