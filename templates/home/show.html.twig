{% extends 'base.html.twig' %}

     {% block metaTitle %}
         " Accueil "
     {% endblock %}

     {% block title %}
         Accueil
     {% endblock %}

     {% block body %}

         <h1 class="h3 mb-3 font-weight-normal"> Accueil </h1>

             <table class="table table-dark table-hover">

                 <tbody>

                     <tr>
                         <th> Id </th>
                         <td> {{ home.id }} </td>
                     </tr>

                     <tr>
                         <th> Titre </th>
                         <td> {{ home.title }} </td>
                     </tr>

                     <tr>
                         <th> Photo </th>
                         <td> {{ home.picture }} </td>
                     </tr>

                     <tr>
                         <th> Courte description </th>
                         <td> {{ home.shortDesc }} </td>
                     </tr>

                     <tr>
                         <th> Longue description </th>
                         <td> {{ home.longDesc }} </td>
                     </tr>

                     <tr>
                         <th> Est actif </th>
                         <td> {{ home.isActive ? 'Oui' : 'Non' }} </td>
                     </tr>

                 </tbody>

             </table>

             <button class="btn btn-primary btn-lg btn-block">
                 <a href="{{ path('home_edit', {'id': home.id}) }}"> Modifier </a><br/>
             </button>   

             {{ include('home/_delete_form.html.twig') }}

             <button class="btn btn-primary btn-lg btn-block">
                 <a href="{{ path('home_index') }}"> Retour à la liste </a><br/>
             </button> 

         <br/>
         <br/>

 {% endblock %}
