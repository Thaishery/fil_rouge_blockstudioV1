{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <h1>Contact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contacte.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ contacte.title }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ contacte.picture }}</td>
            </tr>
            <tr>
                <th>Short_desc</th>
                <td>{{ contacte.shortDesc }}</td>
            </tr>
            <tr>
                <th>Long_desc</th>
                <td>{{ contacte.longDesc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contact_admin_index') }}">back to list</a>

    <a href="{{ path('contact_admin_edit', {'id': contacte.id}) }}">edit</a>

    {{ include('contact_admin/_delete_form.html.twig') }}
{% endblock %}
